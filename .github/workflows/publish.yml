name: Publish
on:
    push:
        branches:
            - releasetest

jobs:
    publish:
        runs-on: ubuntu-latest
        permissions:
            id-token: write # Required for OIDC
            contents: read
        steps:
            - name: Checkout
              uses: actions/checkout@v4
              with:
                  ref: releasetest

            - name: Set Node Version
              uses: actions/setup-node@v4
              with:
                  node-version: '24'
                  registry-url: 'https://registry.npmjs.org'

            - name: Install dependencies
              run: yarn

            - name: Build
              run: npm run-script build:ci

            - name: Publish
              working-directory: ./dist/roosterjs-content-model-types
              run: npm publish
