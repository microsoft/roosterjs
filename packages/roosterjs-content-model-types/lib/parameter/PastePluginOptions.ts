/**
 * Options for PastePlugin
 */
export interface PastePluginOptions {
    /**
     * Whether to replace transparency from images pasted from Word desktop application with white background
     */
    removeTransparencyFromWordDesktopImages?: boolean;
}
