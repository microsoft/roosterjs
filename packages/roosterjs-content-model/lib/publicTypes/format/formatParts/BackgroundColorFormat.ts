/**
 * Format of background color
 */
export type BackgroundColorFormat = {
    /**
     * Background color
     */
    backgroundColor?: string;

    /**
     * Whether we should adjust text color according to lightness of dark color
     */
    adjustTextColor?: boolean;
};
