/**
 * Known announce strings
 */
export enum DefaultAnnounceStrings {
    Min = 1,
    /**
     * String announced when Indenting or Outdenting a list item,
     * @example
     * "Auto corrected, {0}"
     * Where {0} is the new list item bullet
     */
    AnnounceListItemIndentation = 1,
    Max = 1,
}
