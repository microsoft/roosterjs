/**
 * Type of Delimiters, used when adding a Read only & Inline Entity
 */
export enum DelimiterType {
    /**
     * Represents the type of a delimiter that is after the element
     */
    After = 'After',

    /**
     * Represents the type of a delimiter that is before the element
     */
    Before = 'Before',
}
