/**
 * @internal
 * Represent the types of sources to handle in the Paste Plugin
 */
export const enum KnownSourceType {
    WordDesktop,
    ExcelDesktop,
    ExcelOnline,
    PowerPointDesktop,
    GoogleSheets,
    WacComponents,
    Default,
    SingleImage,
}
